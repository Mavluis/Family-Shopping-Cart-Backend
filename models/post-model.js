'use strict';

const mysql = require('mysql2');

const { Schema } = mysql;

/* const postSchema = new Schema({
  id: String,
  notes: String,
  createdAt: {
    type: Date,
    default: Date.now,
  },
  deletedAt: Date,
});

const Post = mysql.model('Post', postSchema);

module.exports = Post; */
